<h1>タスク一覧 (ID : タスク : ステータス)</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task_path(task) %> : <%= task.content %> : <%= task.status%></li>
  <% end %>
</ul>

<%= link_to 'タスクの新規登録', new_task_path %>